import React from 'react';
import './Landing.css'

type IProps = {

}

const Landing: React.FC<IProps> = (props: IProps): JSX.Element => {
    return <header className="header clr-accent flex-stretch">
        hi
    </header>
};

export default Landing;